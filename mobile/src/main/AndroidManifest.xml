<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apps.apene.bioclock_1">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Añadimos permisos para acceder a los sensores del reloj -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tips"></activity>
        <activity
            android:name=".SingleTip"
            android:label="@string/activity_tips" />
        <activity
            android:name=".Graph"
            android:label="@string/activity_graph" />
        <activity
            android:name=".Profile"
            android:label="@string/activity_profile" />
        <activity
            android:name=".Reset"
            android:label="@string/activity_reset" />
        <activity
            android:name=".SignUp"
            android:label="@string/activity_sign_up" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/activity_main" />
    </application>

</manifest>